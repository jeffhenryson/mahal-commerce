/**
 * Ports de saída do domínio <b>logistica</b>.
 *
 * <p><b>Status:</b> esqueleto — contratos declarados, sem adapters ainda.
 * Ports previstos além do {@code ShipmentRepository} (TODO): {@code CarrierPort}
 * (integração com transportadora / geração de etiqueta).</p>
 */
package com.cernecommerce.core.ports.out.logistica;
